#include<stdio.h>
#include<stdlib.h>

int main()
{
    struct player
    {

        char name[22]; //name can be at most 20 characters + 1 NULL + 1 \n check if pdcurses input puts them automatically
        int score;
        //see pdcurses interface DOES it have to be int??
    };
    struct player player1= {'rana', 50 };


//void file(struct player player_num){
    FILE *F1;
    F1=fopen("D:\\CSED 23\\Programming\\Project Game\\Data3.bin","wb+"); //paths //read and writing
    if(F1==NULL)
    {
        printf("ERROR in opening the file \n");
        exit(1);
    }
    fwrite(&player1,sizeof(struct player), 1,F1);  //numbersData???
    if(fwrite != 0)
        printf("contents to file written successfully !\n");
    else
        printf("error writing file !\n");
    //fwirte (&player1,sizeof(struct player), 1 ,*F1);
}
